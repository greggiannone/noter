import { SelectOnShowDirective } from './select-on-show.directive';

describe('SelectOnShowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOnShowDirective();
    expect(directive).toBeTruthy();
  });
});
